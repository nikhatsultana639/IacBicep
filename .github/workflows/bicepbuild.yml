on:
  push:
    branches:
      - main
jobs:
  buld:
    runs-on: ubuntu-latest
    steps:
    - name: bicep-build-output
      uses: Azure/bicep-build-action@v1.0.1
      with:
        bicepFilePath: IacBicep/staticwebapp.bicep
        outputFilePath: IacBicep/azuredeploy.json
          
